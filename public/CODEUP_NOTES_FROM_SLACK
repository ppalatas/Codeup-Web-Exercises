<!-- FORMS 

form method="POST" action="/myfirstform.php"

GET is a download address ----- a server giving a file that you ask for. 
clicking links is a GET request 
typing an address etc 
any GET request is bookmarkable I.E: website 
some forms send GET requests 

the forms that send GET requests are usually searches

these types of requests are called methods 

method == type of request



POST is:
	 Sending DATA




CSS 

EXTRA RESOURCES : 
	W3SCHOOLS 
	MDN
	YOUTUBE:DEVTIPS
	CSS-TRICKS

show cool examples 


CSS 


coolors.co

colorzilla ----- takes colors from a page 



GIT ALIAS ----- ******RESEARCH*****



Expression : is anything or any value or operation or functions or anything that gives you back a simple value

******   sample[sample.length -1]   ******

DO WHILE LOOP******


```// while loop -- lets us run code repeatedly until a condition is met
// general form

while (/* condition */) {
    // statements
    // more statements
}

// how it executes

// 1) check condition
//     2a) if true, execute the body
//          3) go to step 1
//     2b) if false, go to the closing curly brace



// infinite loop -- condition that is always true

var i = 1;
while () {
    console.log("--------------");
    console.log("Start of the while loop! i is " + i);
    console.log("Incrementing i...");
    i += 1;
    console.log("End of the while loop! i is " + i);
}

// counting from 1 to 10 using a while loop
var i = 1;
while (i < 10) {
    console.log(i);
    i += 1;
}

// do-while loop -- while loop that will always execute at least once

do {
    // statements
} while (/* condition */);

// 1) execute body
// 2) check condition
//     3a) if true, go to step 1
//     3b) if false, carry on
```
(edited)

zach [3:11 PM] 
```// this variable represents the number of ice cream cones we have left
var coneInventory = Math.floor(Math.random() * 50) + 50;

do {
    var conesToSell = Math.floor(Math.random() * 5) + 1;    
    // if we have that many left, subtract it from our inventory
    if (conesToSell <= coneInventory) {
        console.log('Sold ' + conesToSell + ' cones...')
        coneInventory = coneInventory - conesToSell;
    } else {
        console.log('I don\'t have enough!')
    }
} while (coneInventory > 0);

console.log("sold all the cones!");```




